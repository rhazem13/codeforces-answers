#include <iostream>
#include <string>
#include <cmath>
#include <algorithm>
using namespace std;
int main()
{
	int n;
	cin >> n;
	int* arr = new int[n];
	int idx;
	for (int i = 0; i < n; i++) {
		cin >> idx;
		arr[idx - 1] = i + 1;
	}
	for (int i = 0; i < n; i++) {
		cout << arr[i]<<" ";
	}
}
