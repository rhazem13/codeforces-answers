#include <iostream>
#include <string>
#include <cmath>
#include <algorithm>
using namespace std;
int main()
{
	int k, r;
	cin >> k >> r;
	int x = 1;
	while (true) {
		if ((x * k) % 10 == 0 || ((x * k)-r) % 10 == 0)
			break;
		x += 1;
	}
	cout << x;
}
